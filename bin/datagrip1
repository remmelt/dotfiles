#!/bin/bash
#Generated by JetBrains Toolbox 1.21.9712 at 2021-08-16T16:11:11.792521

declare -a ideargs=()
declare -- wait=""

for o in "$@"; do
  if [[ "$o" = "--wait" || "$o" = "-w" ]]; then
    wait="-W"
    o="--wait"
  fi
  if [[ "$o" =~ " " ]]; then
    ideargs+=("\"$o\"")
  else
    ideargs+=("$o")
  fi
done

open -na "/Users/remmelt/Library/Application Support/JetBrains/Toolbox/apps/datagrip/ch-1/211.7442.53/DataGrip.app/Contents/MacOS/datagrip" $wait --args "${ideargs[@]}"