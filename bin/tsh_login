#! /usr/bin/env bash

set -Eeuox pipefail

tsh login --auth=default --user=remmelt.pit@bitvavo.com
tsh request new --roles growth-readonly,matching-engine-readonly

tsh db login --db-user=growth-readonly prod-matching-engine-reader
tsh db login --db-user=growth-readonly bitvavo-reader
tsh db login --db-user=growth-readonly prod-growth-mysql-reader

